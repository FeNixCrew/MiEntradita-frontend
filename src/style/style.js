import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
        primary: {
            main: '#2e86c1' 
        },
        secondary: {
            main:  '#3c3c3c'
        },
        text: {
            primary: '#3c3c3c',
        },
        background: {
            paper: '#fff',
        }
    },
});